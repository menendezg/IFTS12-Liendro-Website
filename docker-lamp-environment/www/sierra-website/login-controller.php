<?php 
//WIP. We cant send mails really if we dont use a third party api with auth. 
//We can check this with teacher because this part could be ugly with 
/*this configs.*/


    $name = $_POST['name'];
    $email = $_POST['email'];
    echo $name, $email;

    /*check with simple query select if exist this user and password in the database*/
    //if exist user and password, return ok. 
    /*if dont exist return wrong user and password.*/

?>
